services:
  - type: web
    name: loi-document-service
    runtime: node
    rootDir: document-service
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERVICE_URL
        sync: false  # Set this manually in the Render dashboard to your service's public URL
